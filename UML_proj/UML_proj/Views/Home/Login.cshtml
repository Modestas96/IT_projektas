@model UML_proj.Models.Person

@{
    ViewBag.Title = "Login";
}

@using (Html.BeginForm("Login", "Home", FormMethod.Post))
{
    <fieldset>
        <legend>Login Form to join the UML army</legend>

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        @if (@ViewBag.Message != null)
        {
            <div style="border: 1px solid red">
                @ViewBag.Message
            </div>
        }
        <table>
            <tr>
                <td>@Html.LabelFor(a => a.Vardas)</td>
                <td>@Html.TextBoxFor(a => a.Vardas)</td>
                <td>@Html.ValidationMessageFor(a => a.Vardas)</td>
            </tr>
            <tr>
                <td>
                    @Html.LabelFor(a => a.Slaptažodis)
                </td>
                <td>
                    @Html.PasswordFor(a => a.Slaptažodis)
                </td>
                <td>
                    @Html.ValidationMessageFor(a => a.Slaptažodis)
                </td>
            </tr>
            <tr>
                <td></td>
                <td>
                    <input type="submit" value="Login" />
                </td>
                <td></td>
            </tr>
        </table>
    </fieldset>
}  